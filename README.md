# DocuFlow AI

React + TypeScript + Vite + Tailwind CSS project with Atomic Design.

## Getting Started

1. Clone the repository
2. Install dependencies: `npm install`
3. Run the development server: `npm run dev`
